import React from "react";
import styles from "./app.module.css";

export const App = (): JSX.Element => (
  <div className={styles.app}>
    <header className={styles.header}>
      <h1>React Suspense for RXJS</h1>
    </header>
    <body>

    </body>
  </div>
);
